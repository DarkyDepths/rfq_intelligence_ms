# ── rfq_intelligence_ms — Environment Configuration ──

APP_NAME=rfq_intelligence_ms
APP_PORT=8001
ENVIRONMENT=local
APP_DEBUG=false
CORS_ORIGINS=*

# ── Database (own analytical DB — separate from manager) ──
DATABASE_URL=postgresql+psycopg2://intelligence_user:intelligence_pass@localhost:5433/rfq_intelligence_db

# ── Manager Connector ──
MANAGER_MS_BASE_URL=http://localhost:18000
